package PVE::Service::pveproxy;

# Test stub, used only by the pvedaemon fixture in tests/fixtures so that the
# hook script's `perl -Tc` check has something real to compile.

use strict;
use warnings;

1;
